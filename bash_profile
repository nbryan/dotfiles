PATH=$PATH:$HOME/.homebrew/bin:/usr/local/mysql/bin
export EDITOR="mate -w"
source .git-prompt.sh
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"  # This loads RVM into a shell session.
